apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.name }}-ingress
  namespace: {{ .Chart.Name }}
  labels:
    {{- include "ubuntu.labels" . | nindent 4 }}
spec:
  ingressClassName: "nginx"
  rules:
    - host: "ubuntu2.example.com"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.name }}
                port:
                  number: 80